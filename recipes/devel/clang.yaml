inherit: [cmake, python3-usr, patch]

shared: True

metaEnvironment:
   PKG_VERSION: "19.1.4"

checkoutSCM:
   scm: url
   url: https://github.com/llvm/llvm-project/archive/refs/tags/llvmorg-${PKG_VERSION}.tar.gz
   digestSHA256: 010e1fd3cabee8799bd2f8a6fbc68f28207494f315cf9da7057a2820f79fd531
   stripComponents: 1

checkoutDeterministic: True
checkoutScript: |
   patchApplySeries -p0 $<@clang/*.diff@>

environment:
  LLVM_TARGETS: "${LLVM_TARGETS:-X86;AArch64}"

depends:
   - devel::binutils-dev
   - libs::zlib-dev
   - libs::libffi-dev
   - libs::ncurses-dev

buildVars: [LLVM_TARGETS]
buildTools: [host-toolchain]
buildScript: |
   cmakeBuild $1/llvm \
      -DLLVM_ENABLE_FFI=ON                      \
      -DCMAKE_BUILD_TYPE=Release                \
      -DLLVM_ENABLE_RTTI=ON                     \
      -DLLVM_ENABLE_ZSTD=OFF                    \
      -DLLVM_ENABLE_PROJECTS="clang;lld;clang-tools-extra" \
      -DLLVM_TARGETS_TO_BUILD="${LLVM_TARGETS}" \
      -DLLVM_NATIVE_TOOL_DIR=${BOB_TOOL_PATHS['host-toolchain']} \
      -DLLVM_INCLUDE_BENCHMARKS=OFF             \
      -DCMAKE_BUILD_TYPE=Release \
      -DCLANG_DEFAULT_PIE_ON_LINUX=OFF \
      -DCLANG_ENABLE_CLANGD=On \
      -Wno-dev

packageScript: |
   rsync -a --delete $1/install/ .

   pushd usr/bin

   # Provide wrapper scripts to make clang use the correct target + sysroot.
   # We can not access $BOB_TOOL_PATHS from a wrapper script but we need to pass the
   # gcc-toolchain and the sysroot to clang. To solve this the clang-class provides
   # CLANG_TARGET_TOOLCHAIN.

   cat > "bob_clang" << 'EOF'
   #!/bin/bash
   if [[ -z ${CLANG_TARGET+x} ]]; then
      CLANG_TARGET="--target=${AUTOCONF_TARGET:-${AUTOCONF_HOST}}"
   fi
   exec $(dirname $(realpath "${BASH_SOURCE[0]}"))/clang \
               ${CLANG_TARGET} \
               ${TOOLCHAIN_SYSROOT:+--sysroot=${CLANG_TARGET_TOOLCHAIN}/${TOOLCHAIN_SYSROOT}} \
               ${CLANG_GCC_INSTALL_DIR:+--gcc-install-dir=${CLANG_GCC_INSTALL_DIR}} \
               ${CLANG_TARGET_TOOLCHAIN:+-L ${CLANG_TARGET_TOOLCHAIN}/../lib} \
              "$@" \
              -Wno-unused-command-line-argument \
              -Wno-error=unused-command-line-argument
   EOF

   cat > "bob_clang++" << 'EOF'
   #!/bin/bash
   if [[ -z ${CLANG_TARGET+x} ]]; then
      CLANG_TARGET="--target=${AUTOCONF_TARGET:-${AUTOCONF_HOST}}"
   fi
   exec $(dirname $(realpath "${BASH_SOURCE[0]}"))/clang++ \
               ${CLANG_TARGET} \
               ${TOOLCHAIN_SYSROOT:+--sysroot=${CLANG_TARGET_TOOLCHAIN}/${TOOLCHAIN_SYSROOT}} \
               ${CLANG_GCC_INSTALL_DIR:+--gcc-install-dir=${CLANG_GCC_INSTALL_DIR}} \
               ${CLANG_TARGET_TOOLCHAIN:+-L ${CLANG_TARGET_TOOLCHAIN}/../lib} \
               -stdlib=libstdc++ \
              "$@" \
              -Wno-unused-command-line-argument \
              -Wno-error=unused-command-line-argument
   EOF
   chmod +x bob_clang bob_clang++

provideTools:
  clang: "usr/bin"
