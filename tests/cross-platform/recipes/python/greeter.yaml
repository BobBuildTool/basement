inherit: [ "basement::rootrecipe", python3, install ]

depends:
    - name: python::generator
      use: [tools]
      forward: True

buildTools: [generator]
buildScript: |
    #export PYTHONPYCACHEPREFIX="/tmp"
    export PYTHONDONTWRITEBYTECODE="1"
    generator.py > test.txt

packageScript: |
    installPackageTgt "$1/"
