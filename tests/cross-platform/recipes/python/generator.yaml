inherit: [python3]

buildScript: |
    pipInstall pyparsing

    mkdir -p install/usr/bin
    cat <<EOF > install/usr/bin/generator.py
    #!/usr/bin/env python3
    import pyparsing as pp

    greet = pp.Word(pp.alphas) + "," + pp.Word(pp.alphas) + "!"
    for greeting_str in [
                "Hello, World!",
                "Bonjour, Monde!",
                "Hola, Mundo!",
                "Hallo, Welt!",
            ]:
        greeting = greet.parseString(greeting_str)
        print(greeting)
    EOF

packageScript: |
    installPythonModule

provideTools:
    generator: "usr/bin"
